export type Pillar = {
  glyph: string
  eyebrow: string
  title: string
  body: string
  bullets: string[]
  accent: boolean
}

export type MemoryLoopItem = {
  label: string
  title: string
  body: string
}

export type ContinuityStat = {
  value: string
  label: string
}
